Generate and Build the 9ML iaf_cond_exp_9ml neuron model for NEST
=================================================================

python concept1.py

cd nest_model
./boostrap.sh
mkdir build
cd build
../configure --with-nest=/opt/nest2/bin/nest-config
make -j4
# No need for make install ... can use what resides in .libs
##make install
export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:$HOME/src/nineml-svn-trunk/code_generation/nest2/nest_model/.libs
cd ../..

Test iaf_cond_exp_9ml
=====================

python test_iaf9ml.py


