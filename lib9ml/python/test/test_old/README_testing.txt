To run the Python lib9ml test suite:

$ python tester.py all

For information on how to have microscopic control over the tests run,
follow the instructions provided when running the following command:

$ python tester.py
